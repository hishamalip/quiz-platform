import React from "react";
import SignupForm from './SignupForm';

export default class Asignup extends React.Component {
    render() {
        // main class return
        return (
            <SignupForm />
        );
    }
}
